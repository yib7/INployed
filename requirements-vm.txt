# Scraper VM venv (~/venv on the GCP VM, Python 3.11.2 / Debian).
# Snapshot of the working install on 2026-06-12 (read via pip freeze over SSH);
# pins re-checked against PyPI stable on 2026-07-01. NOTE: numpy must stay on the
# 2.4.x line: numpy 2.5+ requires Python >=3.12 and this VM runs 3.11.2.
#
# Rebuild from scratch:
#   python3 -m venv ~/venv
#   ~/venv/bin/pip install -r requirements-vm.txt
#
# rclone is a system binary on the VM (not pip) — config in ~/.config/rclone/.

aiohttp==3.14.1
google-genai==2.8.0
markdownify==1.2.3
pandas==3.0.3
numpy==2.4.6
